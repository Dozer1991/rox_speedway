# Logs and runtime files
*.log
*.tmp
cache/
cache/*

# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/

# Node modules (if you ever add npm packages for tooling)
node_modules/

# FiveM / fxmanifest cache (some setups generate a cache folder)
fxmanifest.lua.cache

# MySQL config (if you keep credentials in a separate file; adjust as needed)
# config/db_credentials.lua

# Compiled Lua bytecode (never commit .luac)
*.luac

# Anything else you generate locally
*.bak
*.swp
